---
layout: layout
pagination: true
---

<!-- locals: date, articles -->

<%= partial("partials/title", locals: {title: "Monthly Archives: " + date.strftime("%b %Y")}) %>

<%= partial("partials/summary", locals: {articles: (pagination)? page_articles : articles }) %>

<%= partial("partials/pagination", locals: {paginator: paginator}) if pagination %>
